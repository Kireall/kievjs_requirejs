/*global define: true, _: true, $: true */

define(
    [
        'text!path/to/folder/template.html'
    ],
    function (tpl) {
        "use strict";

        // function () { return "<div>...</div>" }

        // var compiledTpl = _.template(tpl);
        // var html = compiledTpl({name: "Jack"});

        // Code here
    }
);
