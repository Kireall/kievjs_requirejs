/*global define: true, _: true, $: true */

define(function () {
    "use strict";

    // No standards + fat client = chaos

    // Clean global namespace

    // Module pattern

    // One module per file

    // Code here

    // Public API
    return {
        param: 'val',
        func: function () {}
    };
});
