/*global define: true, _: true, $: true */

define(
    [
        'amd-1'
    ],
    function (amd1) {
        "use strict";

        // It's same!

        // Function.prototype.toString() to parse and transform
    }
);
