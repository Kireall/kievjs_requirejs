/*global define: true, _: true, $: true */

define(
    [
        'amd-1',
        './subdir/file1',
        '/libs/impress.js',
        '//connect.facebook.net/en_US/all',
        'http://kyivjs.org.ua/jsonp?callback=define'
    ],
    function (amd1, file1, impress, facebookLib, kyivJSjson) {
        "use strict";

        // Dependency management

        // Base path based import

        // Relative path

        // Absolute path

        // Remote path

        // Easy unittestig

        // Code here
    }
);
